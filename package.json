{
  "name": "invoicify",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter @invoicify/api db:migrate"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5"
  }
}
