# sequelize-auto-migrations (github-pinned, unmaintained) require()s sequelize
# from the host project without declaring it as a dependency. npm's flat
# hoisting hid this; pnpm's strict layout does not. Hoist sequelize packages
# to node_modules root so its bare require() calls keep resolving.
public-hoist-pattern[]=*sequelize*
